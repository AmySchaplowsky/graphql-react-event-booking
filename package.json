{
  "name": "graphql-react-event-booking",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon app.js",
    "install:frozen": "rm -rf node_modules/ && yarn install --frozen-lockfile",
    "install:fresh": "rm -rf node_modules/ yarn.lock && yarn install"
  },
  "author": "Amy Schaplowsky",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "express-graphql": "^0.9.0",
    "graphql": "^14.4.1",
    "mongoose": "^5.9.7"
  },
  "devDependencies": {
    "nodemon": "^2.0.2",
    "prettier": "^2.0.4"
  }
}
